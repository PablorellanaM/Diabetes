public class ControladorUsuario {
    // Lógica relacionada con la gestión de usuarios, como registro y autenticación

    public boolean registrarUsuario(String nombre, int edad) {
        // Implementar la lógica de registro en la base de datos
        // Retornar true si el registro es exitoso, false si falla
        return true;
    }

    public boolean autenticarUsuario(String nombre, String contrasena) {
        // Implementar la lógica de autenticación
        // Retornar true si la autenticación es exitosa, false si falla
        return true;
    }
}
